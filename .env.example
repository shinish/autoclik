# Database
DATABASE_URL="file:./prisma/dev.db"

# AWX (Ansible Tower) Configuration (Optional)
# These can also be configured via the Settings page in the UI

# The base URL for your AWX/Ansible Tower instance
# If not set here, configure in Settings > General > AWX Base URL
AWX_BASE_URL=https://awx.example.com/api/v2

# AWX API Token (generate this from your AWX instance)
# If not set here, configure in Settings > General > AWX API Token
AWX_TOKEN=your_awx_token_here

# Configuration Priority:
# 1. Environment variables (.env file) - highest priority
# 2. Database settings (Settings page) - fallback
# 3. Demo mode - if neither is configured, simulates executions

# Note: If AWX is not configured via either method, the application
# will run in demo mode and simulate successful executions for testing.
